# Memory index

- [Mexeu em chatbot/IA/CRM? Atualize os docs, o Eric e o manual sem pedir](feedback_atualizar_docs_eric_manual_sem_pedir.md) — 5 alvos obrigatórios na mesma entrega; zero jargão de banco nos textos; renumerar seção quebra referência cruzada entre os 3 docs (já aconteceu).

- [Agendamento da IA (Ana) no chatbot-engine](architecture_agendamento_ia_chatbot.md) — vitrine curta de horários + válvula de escape pra agenda real; trava anti-repetição por horário citado, não por palavra de recusa; 3 campos de fuso horário não sincronizados (conn.timezone / cfg.timezone / empresa).
- [Taxa de reserva: o motor mandava calar o que o prompt mandava falar](business-rule_taxa_reserva_motor_vs_prompt.md) — regra do prompt do cliente ignorada 3x = procure o bloco do MOTOR que diz o contrário; garantia em `taxa-reserva.ts`; detectar taxa pela PALAVRA falha ("taxa da máquina"), tem que ser pelo VALOR.
- [Conferir as TRÊS pontas antes de dar deploy por concluído](feedback_conferir_as_tres_pontas_do_deploy.md) — banco, motor (edge function) e front sobem por caminhos diferentes; "não mexi nisso hoje" ≠ "está igual ao que testei"; o simulador importa o motor DO DISCO, então bateria só vale se a versão publicada for a mesma.
- [Molde vence proibição no prompt do assistente](feedback_molde_vence_proibicao_no_prompt.md) — proibir não pega, molde pega; exemplo sem buraco vira decoreba e vaza dado ("em maio" para quem nunca falou de maio); texto idêntico entre execuções está sendo COPIADO da base, não gerado; procure na base antes de chamar de alucinação.
- [Base de conhecimento vinda de PDF: tabela achatada faz a IA trocar dado entre produtos](architecture_base_conhecimento_pdf_achatado.md) — parece alucinação e é leitura de tabela destruída; meça a base e cheque duplicata de catálogo ANTES de conferir preço; dado achatado está perdido, preencher é inventar.
- [Reconexão de WhatsApp e a Evolution que nunca apaga](architecture_evolution_reconexao_whatsapp.md) — DEL_INSTANCE desligado em produção; decisão de reaproveitar/renomear/recusar tem que ser por estado observado (com reconfirmação de ~2s), nunca pelo HTTP do logout/delete ter dado certo, senão trava reconexão pra sempre ou orfana sessão viva.
- [Token global: quem reconecta por último derruba clínica de outro](architecture_token_global_ultimo_vence.md) — Meta e Google usavam só o token mais recente; erro (#200) parece permissão do cliente e NÃO é; compare a data que a clínica parou com `updated_at` de `meta_oauth_tokens` ANTES de suspeitar de permissão.
- [Dashboard corta em 1000 linhas e mostra número MENOR que o real](architecture_postgrest_corte_1000_linhas.md) — PostgREST trunca com HTTP 200, sem erro; `isError` não pega; `.limit()` maior não resolve (o corte é no servidor); detectar com `count: 'exact'` e comparar com `data.length`.
- [Versões de API do Google/Meta e onde elas moram](reference_versoes_api_google_meta.md) — v21 desligada 05/08/2026 = 404 com corpo HTML (parece token inválido e não é); hoje v25, sunset ~jul/2027; versão centralizada em `_shared/google.ts`; sem secret sobrescrevendo em produção.
- [Primeiro contato duplicado no `ctwa_events` — a trava e o que ela exige](architecture_ctwa_primeiro_contato_duplicado.md) — a linha `is_first_contact` É o lead; índice único parcial aplicado 01/09/2026 depois de apagar 53 duplicatas; a trava SÓ é segura com o tratamento do 23505 publicado (senão mensagem some e a Evolution não reenvia), e o rebaixamento tem que transferir a origem do anúncio, senão o cartão vira "Direto" e o CAPI não sai.
- [Layout do board do CRM: altura, contain:layout e o eixo de overflow escondido](architecture_kanban_altura_e_overflow.md) — 891 cards sem virtualização = ~96.000px vazando pro documento; altura em `vh` sempre erra (banners de altura variável), `[contain:layout]` no trilho é carga funcional, e declarar `overflow-x` liga o eixo Y sozinho; diagnosticar MEDINDO o DOM, não lendo código.
- [`tsc -b` mascara erros por cache — use `--force`](architecture_tsc_build_cache_mascara_erros.md) — sem `--force` mostra 5 erros, com `--force` mostra 221; quase todos são `types.ts` desatualizado (tabela/coluna faltando), que parece bug do chamador e não é; `npm run build` passa verde mesmo assim.
- [Chave de telefone do CRM tem duas formas — o 9º dígito é a armadilha](architecture_chave_telefone_dois_formatos.md) — comparação tolerante mora em `chavesPhoneBR` (`_shared/atribuicao.ts`); guarda obrigatória contra fundir fixo com celular (fusão faz DELETE em `crm_contacts`, banco sem backup); 4 cópias de lógica de telefone no projeto, 3 deliberadamente não refatoradas.

